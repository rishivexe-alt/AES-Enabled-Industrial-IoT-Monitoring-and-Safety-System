# Hardware Documentation

`prototype_hardware.png` is the representative photograph supplied for the project hardware prototype.

The exact logical pin mapping is documented separately in `hardware/pin_configuration.md`.
